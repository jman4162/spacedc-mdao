# Runtime for the showcase app (streamlit_app.py) for any pip-based install.
# Deliberately slim: orbitdc + the [app] extra (streamlit/plotly/networkx) — no
# panel/kaleido (the [viz] extra) and no heavy solver extras (mdao) or skyfield.
#
# Note: Streamlit Community Cloud installs this repo as a uv project from
# pyproject.toml and does NOT read this file, so the figure libraries are also
# declared in pyproject's default-installed [dependency-groups].app-runtime.
.[app]
