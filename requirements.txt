# Streamlit Community Cloud runtime for the showcase app (streamlit_app.py).
# Deliberately slim: the orbitdc base deps (pydantic/numpy/scipy/pint/pyyaml/
# matplotlib) plus only the two viz libraries the figure builders actually use.
# We do NOT install the [viz] extra here because it pulls panel + kaleido, which
# are unnecessary for Streamlit and risk the free-tier memory ceiling. The heavy
# solver extras (openmdao/pymoo/SALib) and skyfield are intentionally absent.
.
streamlit>=1.40
plotly>=6
networkx>=3
